import { StyleSheet, Dimensions } from 'react-native'

export const styles = StyleSheet.create({
	contentContainer: {
		backgroundColor: '#fff',
		gap: 20,
		minHeight: '100%',
		paddingHorizontal: 5,
		paddingTop: 32
	},
	inputsContainer: {
		gap: 16,
	},
})
